[metadata]
name = "simple_counter"
entry_point = "simple_counter:CounterPlugin"
version = "0.1.0"

[dependencies] # Пример пустой зависимости
# another_plugin = ">=1.0"

[permissions] # Пример разрешений (пока не используются)
# filesystem.read = ["/tmp"]