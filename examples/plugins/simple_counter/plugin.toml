[metadata]
name = "simple_counter"
entry_point = "simple_counter:CounterPlugin"
version = "0.2.0"
title = "Simple counter"
author = "dimnissv"
description = "Plugin with buttons to increase/decrease the value."

[dependencies] # An example of an empty dependence
# another_plugin = ">=1.0"

[permissions] # An example of permits (not yet used)
# filesystem.read = ["/tmp"]